/*
 * JS for map generated by Appery.io
 *
 */

Appery.getProjectGUID = function() {
    return '76c1243c-7c9f-4fcc-bd10-0ef35c0a1435';
}

function navigateTo(outcome, useAjax) {
    Appery.navigateTo(outcome, useAjax);
}

// Deprecated


function adjustContentHeight() {
    Appery.adjustContentHeightWithPadding();
}

function adjustContentHeightWithPadding(_page) {
    Appery.adjustContentHeightWithPadding(_page);
}

function setDetailContent(pageUrl) {
    Appery.setDetailContent(pageUrl);
}

Appery.AppPages = [{
    "name": "CarePass_Login",
    "location": "CarePass_Login.html"
}, {
    "name": "body",
    "location": "body.html"
}, {
    "name": "CarePass_Fitness_Activity_All",
    "location": "CarePass_Fitness_Activity_All.html"
}, {
    "name": "Disclaimer",
    "location": "Disclaimer.html"
}, {
    "name": "CarePass_BodyMeasurements",
    "location": "CarePass_BodyMeasurements.html"
}, {
    "name": "CarePass_Fitness_Activity_New",
    "location": "CarePass_Fitness_Activity_New.html"
}, {
    "name": "CarePass_ErrorPopUp",
    "location": "CarePass_ErrorPopUp.html"
}, {
    "name": "CarePass_Fitness_Activity_Filter",
    "location": "CarePass_Fitness_Activity_Filter.html"
}, {
    "name": "CarePass_BodyMeasurements_New",
    "location": "CarePass_BodyMeasurements_New.html"
}, {
    "name": "details",
    "location": "details.html"
}, {
    "name": "map2",
    "location": "map2.html"
}, {
    "name": "CarePass_BodyMeasurementsType_Filter",
    "location": "CarePass_BodyMeasurementsType_Filter.html"
}, {
    "name": "CarePass_Fitness_Activity_TimeLine",
    "location": "CarePass_Fitness_Activity_TimeLine.html"
}, {
    "name": "map",
    "location": "map.html"
}, {
    "name": "CarePass_Chart",
    "location": "CarePass_Chart.html"
}, {
    "name": "CarePass_Fitness_Activity_Single",
    "location": "CarePass_Fitness_Activity_Single.html"
}, {
    "name": "action",
    "location": "action.html"
}, {
    "name": "load",
    "location": "load.html"
}, {
    "name": "cancer",
    "location": "cancer.html"
}, {
    "name": "tests",
    "location": "tests.html"
}, {
    "name": "start",
    "location": "start.html"
}, {
    "name": "CarePass_BodyMeasurements_Single",
    "location": "CarePass_BodyMeasurements_Single.html"
}, {
    "name": "link",
    "location": "link.html"
}];

map_js = function(runBeforeShow) { /* Object & array with components "name-to-id" mapping */
    var n2id_buf = {
        'mobilebutton_2': 'map_mobilebutton_2',
        'googlemap_3': 'map_googlemap_3',
        'marker_4': 'map_marker_4'
    };

    if ("n2id" in window && window.n2id !== undefined) {
        $.extend(n2id, n2id_buf);
    } else {
        window.n2id = n2id_buf;
    }

    if (navigator.userAgent.indexOf("IEMobile") != -1) {
        //Fixing issue https://github.com/jquery/jquery-mobile/issues/5424 on Windows Phone
        $("div[data-role=page] div[data-role=footer]").css("bottom", "-36px");
    }

    if (Appery.getTargetPlatform == "I") {
        $.each(window.n2id, function(name, id) {
            var elApperyRole = $("#" + id).attr("data-role");
            if (elApperyRole != "appery_label" && elApperyRole != "appery_link") {
                var appleDataSelector = "#" + id + "[x-apple-data-detectors], #" + id + " [x-apple-data-detectors]";
                $(appleDataSelector).attr("x-apple-data-detectors", "false");
            }
        });
    }

    Appery.CurrentScreen = 'map';

    /*
     * Nonvisual components
     */
    var datasources = [];

    geolocation1 = new Appery.DataSource(GeolocationService, {
        'onComplete': function(jqXHR, textStatus) {

            $t.refreshScreenFormElements("map");
        },
        'onSuccess': function(data) {
            var map = Tiggzi('map');
            map.options['address') = "";
            map.refresh();
        },
        'onError': function(jqXHR, textStatus, errorThrown) {},
        'responseMapping': [{
            'PATH': ['coords', 'latitude'],
            'ID': 'googlemap_3',
            'ATTR': 'latitude'
        }, {
            'PATH': ['coords', 'longitude'],
            'ID': 'googlemap_3',
            'ATTR': 'longitude'
        }],
        'requestMapping': [{
            'PATH': ['options', 'maximumAge'],
            'TYPE': 'STRING',
            'ATTR': '3000'
        }, {
            'PATH': ['options', 'timeout'],
            'TYPE': 'STRING',
            'ATTR': '5000'
        }, {
            'PATH': ['options', 'enableHighAccuracy'],
            'TYPE': 'STRING',
            'ATTR': 'true'
        }, {
            'PATH': ['options', 'watchPosition'],
            'TYPE': 'STRING',
            'ATTR': 'false'
        }]
    });

    datasources.push(geolocation1);

    /*
     * Events and handlers
     */

    // Before Show
    map_beforeshow = function() {
        Appery.CurrentScreen = "map";
        for (var idx = 0; idx < datasources.length; idx++) {
            datasources[idx].__setupDisplay();
        }
    }

    // On Load
    screen_85D7_onLoad = map_onLoad = function() {
        screen_85D7_elementsExtraJS();

        // TODO fire device events only if necessary (with JS logic)
        map_deviceEvents();
        map_windowEvents();
        screen_85D7_elementsEvents();
    }

    // screen window events
    screen_85D7_windowEvents = map_windowEvents = function() {

        $('#map').bind('pageshow orientationchange', function() {
            var _page = this;
            adjustContentHeightWithPadding(_page);
        });

    }

    // device events
    map_deviceEvents = function() {

        document.addEventListener("deviceready", function() {

        });
    }

    // screen elements extra js
    screen_85D7_elementsExtraJS = map_elementsExtraJS = function() {
        // screen (map) extra code

        /* googlemap_3 */

        $("[name = 'googlemap_3']").wrap("<div/>");
        $("[name = 'googlemap_3']").parent().css("margin-left", $("[name = 'googlemap_3']").css("margin-left"));
        $("[name = 'googlemap_3']").parent().css("margin-right", $("[name = 'googlemap_3']").css("margin-right"));
        $("[name = 'googlemap_3']").css("margin-left", '0');
        $("[name = 'googlemap_3']").css("margin-right", '0');

        var googlemap_3_options = {
            markerSourceName: "googlemap_3_markers",
            latitude: "",
            longitude: "",
            address: "New York, NY",
            zoom: 10,
            showLocationMarker: true
        }

        Appery.__registerComponent('googlemap_3', new Appery.ApperyMapComponent("googlemap_3", googlemap_3_options));
        $("[name='googlemap_3_markers'] [apperytype='marker']").attr("reRender", "googlemap_3");
        $("[name='googlemap_3']").closest("[data-role='page']").bind({
            pageshow: function() {
                if (Appery('googlemap_3') != undefined) {
                    Appery('googlemap_3').refresh();
                }
            }
        });

    }

    // screen elements handler
    screen_85D7_elementsEvents = map_elementsEvents = function() {

        $("a :input,a a,a fieldset label").live({
            click: function(event) {
                event.stopPropagation();
            }
        });

        $('#map_mobilecontainer [name="mobilebutton_2"]').die().live({
            click: function() {
                if (!$(this).attr('disabled')) {
                    try {
                        geolocation1.execute({})
                    } catch (ex) {
                        console.log(ex.name + '  ' + ex.message);
                        hideSpinner();
                    };

                }
            },
        });

    }

    $("#map").die("pagebeforeshow").live("pagebeforeshow", function(event, ui) {
        map_beforeshow();
    });

    if (runBeforeShow) {
        map_beforeshow();
    } else {
        map_onLoad();
    }

}

$("#map").die("pageinit").live("pageinit", function(event, ui) {
    Appery.processSelectMenu($(this));
    map_js();
});